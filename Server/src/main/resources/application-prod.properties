# Production overrides for push notifications & related deployment settings.
# Load by setting `spring.profiles.active=prod`.

# Database Configuration
# TODO(deploy): Inject connection details via environment/secrets manager.
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# JWT Configuration
# TODO(deploy): Provide secure JWT secrets via environment variables.
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION:86400000}

push.dispatch.enabled=true

# VAPID credentials (replace before deploying to production).
# TODO(deploy): Rotate these keys via secrets manager and do not commit real prod values.
push.vapid.public-key=BLvt0WNCKhjo54kvt4q0k85sBJnXVRaNhg2XKIrztma3FgoFfJrnLZY0gBg6U2f2KD-86EYAfrvg7Dt1VWuPxJI
push.vapid.private-key=O-D9aIzoMhLfeNHloSvXXM4vRkmK1xJ1kbYuhGPaOSA
push.vapid.subject=mailto:support@mentoringplatform.com

# Add further production-only properties here (e.g., logging, API hosts).
